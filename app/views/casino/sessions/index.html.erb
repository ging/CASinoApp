<div class="container">
  <div class="sessions box">
    <%= render 'messages' %>
    <div class="info">
      <h1><%= t('sessions.title') %></h1>
      <p>
        <%= t('sessions.currently_logged_in_as') + " " + current_user.username %>
      </p>

      <ul class="nav navbar-nav">
        <li class="btn btn-default btn-header btn-home">
          <a class="linker" href="<%=CASinoApp::Application.config.APP_CONFIG["vish_url"]%>"><%=t('sessions.label_vish')%></a>
        </li>
        <li class="btn btn-default btn-header btn-home">
          <a class="linker" href="<%=CASinoApp::Application.config.APP_CONFIG["moodle_url"]%>"><%=t('sessions.label_moodle')%></a>
        </li>
        <li class="btn btn-default btn-header btn-home">
          <a class="linker" href="<%=logout_path%>"><%=t('sessions.label_logout_button')%></a>
        </li>

      </ul>

    </div>

  </div>
  <%= render 'footer' %>
</div>
